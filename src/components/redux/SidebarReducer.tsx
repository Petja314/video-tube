import React from 'react';

export type SidebarStateReducerType = {
    state : any
}
export type ActionSidebarReducerType = {
    action : any
}

export const SidebarReducer = (state : SidebarStateReducerType ,action : ActionSidebarReducerType ) => {
        
    return state;
};

